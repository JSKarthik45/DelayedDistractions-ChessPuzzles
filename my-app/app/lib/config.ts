// Development-only switches. Flip to false before shipping.
export const FORCE_ONBOARDING_EVERY_TIME = true;

// This default export is only to satisfy Expo Router's route file expectations
// for files under the app/ directory. This file is NOT a real route.
export default function _IgnoreRoute() {
	return null;
}
